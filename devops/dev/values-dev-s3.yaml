components:
  - name: s3
    image:
      name: infra/front/tech-writers/docs-public/s3
    replicas: 1
    ports:
      - port: 3000
        headlessOnly: true
    probes:
      startup:
        path: s3/api/healthz
        port: 3000
        initialDelaySeconds: 30
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 60
      liveness:
        path: s3/api/healthz
        port: 3000
        initialDelaySeconds: 20
        periodSeconds: 30
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
      readiness:
        path: s3/api/healthz
        port: 3000
        initialDelaySeconds: 20
        periodSeconds: 15
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
    ingress:
      className: "nginx-vk-docs-share"
      rules:
        - hostname: s3-{{ .Release.Store.ingressHostname }}
          paths:
            - path: /s3
      tls:
        - secretName: wildcard-tls-secret
