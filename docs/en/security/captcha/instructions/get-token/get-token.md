# {heading(Getting a service token)[id=captcha-get-token]}

{include(/en/_includes/_translated_by_ai_en.md)}

To get the service token `service_token` for accessing VK Капча, register in the service. To register, contact the [sales department](https://cloud.vk.ru/contacts/). If necessary, specify:

{include(../../../../_includes/_captcha-get-token.md)[tags=captcha-get-token]}