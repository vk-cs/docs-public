# {heading(Karboii component events)[id=event-log-karboii]}

Events that the {linkto(../../../../../storage/backups/concepts/about#backup-about)[text=backup service]} reports to Cloud Audit.

The tables list the short resource name. The full name includes the `vkcloud.cloud_backup` prefix.

## {heading(Domain events)[id=event-log-domain-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=domain-events]}

## {heading(API request events)[id=event-log-api-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=api-events]}
