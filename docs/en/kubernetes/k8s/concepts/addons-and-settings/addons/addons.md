{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=addons]}

## {heading(Features of installing add-ons)[id=features_of_installing_addons]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=install-features]}

## {heading(Available add-ons)[id=available_addons]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=available]}

### Capsule

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=capsule]}

### cert-manager

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=cert-manager]}

### Docker Registry

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=docker-registry]}

### Fluent Bit

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=fluent-bit]}

### Fluent Bit for Cloud Logging (logaas-integration)

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=fluent-bit-logaas-magnum]}

### {heading(GPU Operator)[id=gpu_operator]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=gpu-operator]}

### {heading(Ingress Controller (NGINX))[id=ingress]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=ingress-controller-nginx]}

### {heading(Istio)[id=istio]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=istio]}

### {heading(Jaeger)[id=jaeger]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=jaeger]}

### Kiali

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=kiali]}

### {heading(Kube Prometheus Stack)[id=kube_prometheus_stack]}

{include(/en/kubernetes/mk8s/_includes/_addons.md)[tags=kube-prometheus-stack]}
