{note:warn}

Service users are created in the project along with the creation of an instance. The names of these users begin with `mpl_user_`. They are used to create and renew certificates.

Do not delete these users, as this will make the service inoperable.

{/note}