1. Study the [official Argo CD documentation](https://argo-cd.readthedocs.io/en/stable/) to avoid mistakes when configuring and working with the add-on. Particularly, when using the add-on, you will need to be able to:

    - access the Argo CD web interface
    - connect the Git repository that contains the configuration of the cluster
    - start manual or automatic synchronization of the cluster state with its Git repository