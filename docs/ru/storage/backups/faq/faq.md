# {heading(Вопросы и ответы)[id=backup-faq]}

{cut(Как сделать резервную копию ВМ?)}

Создать резервную копию ВМ можно {linkto(../instructions/create-backup-copy#backup-copy-create)[text=вручную]} или {linkto(../instructions/create-backup-plan#backup-plan-create)[text=при помощи плана резервного копирования]}.

{/cut}

{ifdef(public)}
{cut(Сколько резервных копий может храниться?)}

Максимальное количество хранимых автоматических резервных копий — 200 штук. При достижении установленного лимита хранения старые резервные копии будут автоматически удаляться.

{/cut}

{cut(Не потеряются ли резервные копии?)}

Резервные копии хранятся в защищенном объектном хранилище, которое автоматически реплицируется для сохранения целостности данных.

Резервные копии не удаляются при сбоях оборудования или сервиса.

{/cut}
{/ifdef}

{cut(Сколько времени занимает создание резервной копии?)}

Время создания резервной копии ВМ зависит от следующих факторов:

- объем информации на подключенных к ВМ дисках, для которых выполняется резервное копирование;
- {ifdef(public)}{linkto(../../../computing/iaas/concepts/data-storage/volume-sla#iaas-volume-sla)[text=производительность]}{/ifdef}{ifndef(public)}производительность{/ifndef} этих дисков.

Время резервного копирования базы данных зависит от ее размера.

Для резервных копий, {linkto(../instructions/create-backup-plan#backup-plan-create)[text=создаваемых автоматически]} по заранее настроенному плану, время создания можно рассчитать как разницу между временем завершения и запуска резервного копирования. Эти данные можно найти в разделе **Cloud Backup → Резервное копирование** в {linkto(../instructions/manage-backup-plan#backup-plan-view-copies-list)[text=списке резервных копий плана]}.

{/cut}

{cut(Как удалить резервную копию?)}

Удалить резервную копию можно в разделе **Cloud Backup → Резервное копирование**, подробнее в статье {linkto(../instructions/manage-backup-copy#backup-copy-delete)[text=Управление резервными копиями]}.

{/cut}

{cut(Что происходит, если превышен указанный в плане резервного копирования лимит копий?)}

Чтобы записывать новые копии, {var(cloud)} удаляет старые согласно {linkto(../concepts/about#backup-about)[text=политике]} плана резервного копирования.

{/cut}

{cut(Невозможно выполнить резервное копирование ВМ)}

Например, отсутствует пункт меню или виртуальной машины нет в списке. Проблема может возникнуть, если в операционной системе ВМ нет гостевого агента QEMU (`qemu guest-agent`). 

После установки необходимо прописать на ВМ дополнительные метаданные. В некоторых случаях может потребоваться перезагрузка ВМ. 

{ifndef(private-pdf, private-pg-pdf)}
Подробнее о решении проблемы — в статье {linkto(../troubleshooting/vm-backup-unavailable#backup-vm-backup-unavailable)[text=%text]}.
{/ifndef}

{/cut}

{cut(Как посмотреть список ВМ без плана резервного копирования?)}

{linkto(../../../computing/iaas/instructions/vm/vm-filter#iaas-vm-filter)[text=Используйте]} фильтр **Бэкап** для поиска таких виртуальных машин.

{/cut}
