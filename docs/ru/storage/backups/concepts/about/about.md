# {heading(О сервисе)[id=backup-about]}
{ifndef(private-cert)}
Cloud Backup — сервис, в котором можно централизованно управлять созданием резервных копий и восстанавливать из них различные облачные объекты:

- виртуальные машины;
  {/ifndef}
  {ifdef(public)}
- инстансы аналитических баз данных;
  {/ifdef}
  {ifndef(private-cert)}
- инстансы баз данных.
  {/ifndef}

{ifdef(private-cert)}
Cloud Backup — сервис, в котором можно централизованно управлять созданием резервных копий виртуальных машин и восстанавливать из них ВМ.
{/ifdef}

Cloud Backup позволяет:

- {linkto(../../instructions/create-backup-copy#backup-copy-create)[text=запускать резервное копирование вручную]}, например, перед операциями, связанными с риском утраты данных;
- {linkto(../../instructions/create-backup-plan#backup-plan-create)[text=создавать резервные копии автоматически]} по заранее настроенному расписанию.

Для копирования по расписанию Cloud Backup предоставляет выбор между стратегиями создания и хранения резервных копий:

- Стратегия {linkto(../forward-incremental#backup-forward-incremental)[text=с ограничением по максимальному числу]} хранимых полных копий.
- {linkto(../gfs-backup#backup-gfs)[text=GFS-стратегия]}.
  {ifdef(public)}
- Создание резервных копий с {linkto(../blocked-backup#backup-blocked)[text=блокировкой (защитой) от удаления]}.

Для СУБД PostgreSQL также поддерживается механизм непрерывного архивирования и восстановления на момент времени ({linkto(../../instructions/point-in-time-recovery-pitr#backup-pitr)[text=Point-in-Time Recovery, PITR]}).
{/ifdef}

Резервные копии хранятся в объектном хранилище {ifdef(public)}{var(s3)} в автоматически создаваемых бакетах с {linkto(../../../s3/concepts/about#s3-concepts-about)[text=классом хранения]} `BackupBucket`{/ifdef}{ifndef(public)}(или ином, в зависимости от настроек){/ifndef}.

При настройке резервного копирования виртуальных машин можно выбрать, какие подключенные к ВМ диски сохранятся в ее резервной копии. Исключив из резервной копии часть дисков, например большие data-диски, вы можете сократить время резервного копирования и уменьшить размер копии. Выбор дисков доступен и в ручном, и в автоматическом режиме резервного копирования.

На скорость восстановления ВМ из резервной копии влияют:

- Тип резервной копии — восстановление из инкрементальной копии занимает больше времени, так как при этом сначала восстанавливаются данные из полной копии, а затем последовательно добавляются изменения из более поздних инкрементальных копий.

- Тип диска — чем {ifdef(public)}{linkto(../../../../computing/iaas/concepts/data-storage/volume-sla#iaas-volume-sla)[text=быстрее диск]}{/ifdef}{ifndef(public)}быстрее диск{/ifndef} целевой ВМ, тем быстрее будет происходить восстановление.

- Тип восстановления — восстановление в исходную ВМ занимает меньше времени.