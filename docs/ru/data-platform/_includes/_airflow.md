{includetag(conf)}

- Single: экземпляр сервиса, развернутый на одном сервере. Решение подходит для разработки и тестирования, снижает расходы на развертывание тестовой инфраструктуры. Не обеспечивает отказоустойчивость: сбой узла приводит к остановке всей работы.
- Кластер: отказоустойчивая конфигурация из нескольких узлов (сбой одного узла не приводит к остановке инстанса Cloud Airflow). Решение обеспечивает высокую производительность и подходит для хранения баз данных, в том числе для высоконагруженных проектов со строгими требованиями к сохранности данных.

{/includetag}

<!-- Роли в Cloud Airflow -->

{includetag(roles_airflow)}
- `Администратор` — роль с максимально широким набором прав. Администратор может создавать, изменять и удалять любые объекты (DAG, подключения, пулы, переменные), управлять пользователями и их ролями, просматривать и изменять конфигурацию.
- `Пользователь` — роль с доступом к управлению объектами Cloud Airflow. Пользователь может создавать, изменять и удалять DAG, запускать и останавливать задачи, просматривать логи выполнения, управлять подключениями, пулами и переменными.
- `Оператор` — роль, расширяющая возможности роли Пользователь. Оператор дополнительно может очищать историю задач, управлять блокировками DAG, просматривать и изменять конфигурацию раннеров, управлять слотами выполнения задач.
- `Наблюдатель` — роль с ограниченным доступом на чтение. Наблюдатель может просматривать DAG, статус их выполнения, логи задач и базовую информацию об объектах Cloud Airflow, но не может создавать, изменять или удалять объекты, запускать или останавливать задачи.
{/includetag}

<!-- Параметры подключений Cloud Airflow -->

{includetag(connections_airflow)}

{tabs}

{tab(Подключение к хранилищу VK Cloud S3)}

- **Название подключения**: название подключения.
- **Bucket**: имя бакета, в котором будут храниться DAG-файлы или логи DAG.
- **Путь к файлам в bucket**: путь до бакета.

{/tab}

{tab(Подключение к внешнему S3 хранилищу)}

- **Название подключения**: название подключения.
- **Регион**: регион, в котором расположено хранилище S3.
- **Access Key**: публичный ключ для доступа к хранилищу.
- **Secret Key**: приватный ключ для доступа к хранилищу.
- **S3 URL**: адрес хранилища S3. Например: `https://hb.ru-msk.vkcloud-storage.ru`.
- **Bucket**: имя бакета, в котором будут храниться DAG-файлы или логи DAG.
- **Путь к файлам в bucket**: путь до бакета.

{/tab}

{/tabs}

{/includetag}