# {heading(О сервисе)[id=clickhouse_info]}

{include(../../../_includes/_clickhouse.md)[tags=intro-dp]}

Список версий ClickHouse, которые поддерживаются в {var(cloud)}, доступен при {linkto(../../instructions/create#clickhouse_create)[text=создании экземпляра]} этого сервиса.

Подробнее о ClickHouse — в [официальной документации](https://clickhouse.com/docs/ru).
