# {heading(Работа с компонентами сервиса)[id=kafka_management]} 

{ifndef(public)}
{include(../../../_includes/_standalone.md)[tags=difference_management]}
{/ifndef}

## {heading(Просмотр информации об экземпляре)[id=kafka_view]}

{tabs}

{tab(Личный кабинет)}

{ifdef(public)}
1. [Перейдите](https://msk.cloud.vk.com/app/) в личный кабинет VK Cloud.
{/ifdef}
   
1. Перейдите в раздел **Data Platform** → **Экземпляры сервисов**.
1. Нажмите на имя нужного экземпляра.
1. Просмотрите информацию на вкладках страницы экземпляра:

   - **Общая информация** — параметры экземпляра и основные эндпоинты.
   - **Архитектура** — узлы Cloud Kafka и их характеристики, представленные в виде схемы.
   - **Мониторинг** — просмотр метрик экземпляра сервиса.
   - **Учетные данные** — просмотр учетных записей и управление ими.
   - **Настройки** — просмотр и редактирование конфигурации сервиса.

      Чтобы ознакомиться с описанием определенного параметра, нажмите на его название.

   - **Обслуживание** — просмотр и управление параметрами обслуживания Cloud Kafka.


{/tab}

{/tabs}

## {heading(Редактирование имени и описания для экземпляра)[id=kafka_edit]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_edit_name]}

{/tab}

{/tabs}

{ifndef(public)}

## {heading(Назначение внешнего IP)[id=kafka_assign-ip]}

{note:warn}
Доступно только для {var(data-p)} в составе {var(cloud)}.
{/note}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_assign_ip]}

{/tab}

{/tabs}

## {heading(Изменение IP и портов)[id=kafka_change-ip]}

{note:warn}
Только для Standalone и Standalone в составе {var(cloud)}.
{/note}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_standalone.md)[tags=change_ip]}

{/tab}

{/tabs}

{/ifndef}

## {heading(Увеличение размера диска)[id=kafka_disk-resize]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_disk_resize]}

{/tab}

{/tabs}

## {heading(Горизонтальное масштабирование)[id=kafka_scaling]}

{tabs}

{tab(Личный кабинет)}

{ifdef(public)}
1. [Перейдите](https://msk.cloud.vk.com/app/) в личный кабинет VK Cloud.
{/ifdef}
   
1. Перейдите в раздел **Data Platform → Экземпляры сервисов**.
1. Перейдите в настройки масштабирования одним из способов.

   - Из списка экземпляров сервисов:

     1. В списке экземпляров сервисов нажмите на значок **•••** для нужного экземпляра.
     1. Выберите пункт **Горизонтальное масштабирование**.

   - На странице экземпляра сервиса:

     1. В списке экземпляров сервисов нажмите на имя нужного экземпляра.
     1. На вкладке **Общая информация** нажмите на значок **•••**.
     1. Выберите пункт **Горизонтальное масштабирование**.

1. Укажите новое количество узлов брокера Kafka.
1. Добавьте или удалите дополнительные опции:

   - **Kafka Bridge** — для работы с Cloud Kafka через REST API;
   - **Kafka UI** — для работы с Cloud Kafka через графический интерфейс.

1. Нажмите кнопку **Сохранить изменения**.

{/tab}

{/tabs}

## {heading(Редактирование конфигурации сервиса)[id=kafka_config]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_edit_settings_new]}

{/tab}

{/tabs}

## {heading(Изменение параметров обслуживания)[id=kafka_maintenance]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_service]}

{/tab}

{/tabs}

## {heading(Добавление учетной записи)[id=kafka_user-add]}

{tabs}

{tab(Личный кабинет)}

{ifdef(public)}
1. [Перейдите](https://msk.cloud.vk.com/app/) в личный кабинет {var(cloud)}.
{/ifdef}
   
1. Перейдите в раздел **Data Platform → Экземпляры сервисов**.
1. В списке экземпляров сервисов нажмите на имя нужного экземпляра.
1. На странице экземпляра перейдите на вкладку **Учетные данные**.
1. Нажмите кнопку **Добавить учетную запись**.
1. В открывшемся окне задайте логин пользователя для доступа к экземпляру Cloud Kafka.
1. Выберите роль пользователя:

   {include(../../../_includes/_kafka.md)[tags=roles]}

1. Придумайте или сгенерируйте пароль пользователя.

   Требования к паролю:

   - может содержать только цифры, символы `!`, `"`, `#`, `$`, `%`, `&`, `'`, `(`, `)`, `*`, `+`, `,`, `-`, `.`, `/`, `:`, `;`, `<`, `=`, `>`, `?`, `@`, `[`, `\`, `]`, `^`, `_`, `` ` ``, `{`, `|`, `}`, `~`, заглавные и строчные латинские буквы;
   - должен состоять как минимум из 16 символов;
   - максимальная длина пароля 50 символов;
   - должен содержать хотя бы одну заглавную и одну строчную букву латинского алфавита, хотя бы одну цифру.

   {note:err}
   Восстановить пароль нельзя.
   {/note}

1. Нажмите кнопку **Сохранить**.

{/tab}

{/tabs}

## {heading(Ограничение доступа учетной записи)[id=kafka_user-access]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_user_access]}

{/tab}

{/tabs}

## {heading(Удаление учетной записи)[id=kafka_user-delete]}

{tabs}

{tab(Личный кабинет)}

{include(../../../_includes/_data_p.md)[tags=instance_user_delete]}

{/tab}

{/tabs}

## {heading(Удаление экземпляра сервиса)[id=kafka_delete]}

{tabs}

{tab(Личный кабинет)}


{include(../../../_includes/_data_p.md)[tags=instance_delete]}

{/tab}

{/tabs}
