## {heading(Добавление правила доступа)[id=add]}

{tabs}

{tab(Личный кабинет)}

{note:info}

Правила добавляются по одному. Одновременное добавление нескольких правил не поддерживается.

{/note}

{include(/ru/_includes/_trino.md)[tags=open]}

1. Перейдите на вкладку **Доступы**.
1. Нажмите кнопку **Добавить правило**.
1. Выберите тип доступа и установите параметры правила:

    {tabs}

    {tab(Каталог)}

    - **Название**: произвольное имя для правила.
    - **Пользователи**: список пользователей, на которых распространяется правило. По умолчанию правило распространяется на всех пользователей.
    - **Привилегии**: 

        - `None` — запретить доступ.
        - `All` — полный доступ ко всем операциям.
        - `Read only` — доступ только на чтение.

    - **Каталоги**: названия каталогов, на которые распространяется правило. По умолчанию правило распространяется на все каталоги. Вводятся вручную и должны совпадать с названиями каталогов, которые можно [узнать](/ru/data-platform/trino/instructions/management#view-connect) в настройках подключения или с помощью запроса:

        ```sql
        SHOW CATALOGS;
        ```

    {/tab}

    {tab(Схема)}

    - **Название**: произвольное имя для правила.
    - **Пользователи**: список пользователей, на которых распространяется правило. По умолчанию правило распространяется на всех пользователей.
    - **Привилегии**: разрешает доступ при значении `True` и запрещает его при значении `False`.
    - **Каталоги**: названия каталогов, на которые распространяется правило. По умолчанию правило распространяется на все каталоги. Вводятся вручную и должны совпадать с названиями каталогов, которые можно [узнать](/ru/data-platform/trino/instructions/management#view-connect) в настройках подключения или с помощью запроса:

        ```sql
        SHOW CATALOGS;
        ```

    - **Схемы**: названия схем, на которые распространяется правило. По умолчанию правило распространяется на все схемы. Вводятся вручную и должны совпадать с названиями схем в экземпляре Cloud Trino. Узнать названия схем можно с помощью запроса:

        ```sql
        SHOW SCHEMAS FROM <ИМЯ_КАТАЛОГА>;
        ```

        {note:info}

        Правило распространяется на все схемы с таким названием во всех указанных каталогах.

        {/note}

    {/tab}

    {tab(Таблица)}

    - **Название**: произвольное имя для правила.
    - **Пользователи**: список пользователей, на которых распространяется правило. По умолчанию правило распространяется на всех пользователей.
    - **Привилегии**: привилегии на действия с таблицами. Можно выбрать как одну, так и несколько. Доступны следующие привилегии:

        - `SELECT` — просмотр и чтение данных из таблиц.
        - `INSERT` — добавление новых данных в таблицы.
        - `DELETE` — удаление данных из таблиц.
        - `UPDATE` — изменение данных в таблицах.
        - `OWNERSHIP` — полный контроль над таблицами, включая изменение структуры и управление правами доступа.
        
    - **Каталоги**: названия каталогов, на которые распространяется правило. По умолчанию правило распространяется на все каталоги. Вводятся вручную и должны совпадать с названиями каталогов, которые можно [узнать](/ru/data-platform/trino/instructions/management#view-connect) в настройках подключения или с помощью запроса:

        ```sql
        SHOW CATALOGS;
        ```

    - **Схемы**: названия схем, на которые распространяется правило. По умолчанию правило распространяется на все схемы. Вводятся вручную и должны совпадать с названиями схем в экземпляре Cloud Trino. Узнать названия схем можно с помощью запроса:

        ```sql
        SHOW SCHEMAS FROM <ИМЯ_КАТАЛОГА>;
        ```

        {note:info}

        Правило распространяется на все схемы с таким названием во всех указанных каталогах.

        {/note}

    - **Таблицы**: названия таблиц, на которые распространяется правило. По умолчанию правило распространяется на все таблицы. Вводятся вручную и должны совпадать с названиями таблиц в экземпляре Cloud Trino. Узнать названия таблиц можно с помощью запроса:

        ```sql
        SHOW TABLES FROM <ИМЯ_КАТАЛОГА>.<ИМЯ_СХЕМЫ>;
        ```

        {note:info}

        Правило распространяется на все таблицы с таким названием во всех указанных схемах и каталогах.

        {/note}

    {/tab}

    {/tabs}

1. Нажмите кнопку **Добавить**.

{/tab}

{/tabs}

## {heading(Просмотр правил доступа)[id=view]}

{tabs}

{tab(Личный кабинет)}

{include(/ru/_includes/_trino.md)[tags=open]}

1. Перейдите на вкладку **Доступы**.
1. Ознакомьтесь с правилами доступа для каталогов, схем и таблиц.

{/tab}

{/tabs}

## {heading(Изменение последовательности правил доступа)[id=rearrange]}

Правила применяются по порядку сверху вниз. Размещайте общие правила выше, а более точечные правила ниже общих.

{tabs}

{tab(Личный кабинет)}

{include(/ru/_includes/_trino.md)[tags=open]}

1. Перейдите на вкладку **Доступы**.
1. Нажмите кнопку **Упорядочить**.
1. Измените порядок правил для каталогов, схем или таблиц.
1. Нажмите кнопку **Сохранить**.

{/tab}

{/tabs}

## {heading(Редактирование правила доступа)[id=edit]}

{tabs}

{tab(Личный кабинет)}

{include(/ru/_includes/_trino.md)[tags=open]}

1. Перейдите на вкладку **Доступы**.
1. Нажмите ![more-icon](/ru/assets/more-icon.svg "inline") справа от правила и выберите пункт **Редактировать**.
1. Отредактируйте правило и нажмите кнопку **Сохранить**.

{/tab}

{/tabs}

## {heading(Удаление правила доступа)[id=delete]}

{tabs}

{tab(Личный кабинет)}

{include(/ru/_includes/_trino.md)[tags=open]}

1. Перейдите на вкладку **Доступы**.
1. Нажмите ![more-icon](/ru/assets/more-icon.svg "inline") справа от правила и выберите пункт **Удалить**.
1. Подтвердите удаление правила.

{/tab}

{/tabs}