# {heading(Термины и сокращения)[id=s3-concepts-terms]}

{ifdef(s3-pdf)}
В настоящем документе используются сокращения, перечисленные в {linkto(#tab_terms)[text=таблице %number]}.

{caption(Таблица {counter(table)[id=numb_tab_terms]} — Термины и сокращения)[align=right;position=above;id=tab_terms;number={const(numb_tab_terms)}]}
{/ifdef}
[cols="1,3", options="header"]
|===
|Сокращение
|Расшифровка

|ACL
|Access Control List, список управления доступом, который определяет, кто или что может получать доступ к объекту, и какие именно операции разрешено или запрещено выполнять субъекту

|API
|Application Programming Interface — аппаратно-программный интерфейс. Описание способов (набор классов, процедур, функций, структур или констант), которыми одна компьютерная программа может взаимодействовать с другой программой

|AWS
|Amazon Web Services — облачное объектное хранилище, предоставляющее сервис хранения объектов Amazon Simple Storage Service (Amazon S3)

|AWS CLI
|Интерфейс командной строки AWS. Используется для управления сервисами {var(s3)}

|JavaScript
|Мультипарадигменный язык программирования. Поддерживает объектно-ориентированный, императивный и функциональный стили

|JSON
|JavaScript Object Notation — текстовый формат обмена данными, основанный на JavaScript

|HashiCorp Vault
|Инструмент, обеспечивающий безопасное хранение и доступ к различным секретам, например, паролям

|IAM
|Инструмент администрирования через веб-интерфейс (Web UI).

|KMS
|Key Management Service — служба централизованного управления ключами шифрования. В качестве KMS {var(s3)} используется Hashicorp Vault

|S3
|Simple Storage Service. Сервис хранения объектов

|SSE
|Server-side encryption, шифрование на стороне сервера. Каждый объект в {var(s3)} шифруется уникальным ключом с помощью KMS (SSE-E) или ключом, предоставленным пользователем (SSE-C)

|URL
|Uniform Resource Locator, унифицированный указатель ресурса — система унифицированных адресов электронных ресурсов, или единообразный определитель местонахождения ресурса. Используется как стандарт записи ссылок на объекты в Интернете

|ОС
|Операционная система

|pub/sub
|Модель работы уведомлений издатель-подписчик (publisher-subscriber)

|push-модель
|Модель мгновенной отправки уведомлений при выполнении заданных условий в конфигурациях

|SNS
|Simple Notification Service, механизм автоматической отправки уведомлений (через HTTP-запросы) на указанные URL

|EventBridge
|Сервис по отправке уведомлений
|===
{ifdef(s3-pdf)}
{/caption}
{/ifdef}