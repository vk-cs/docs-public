# {heading(Redis)[id=dbaas-redis]}

## {heading(Запуск, перезапуск и остановка инстанса БД)[id=dbaas-redis-start-restart]}

{ifdef(public)}
{note:info}
Инстанс БД в конфигурации **Cluster** нельзя запустить, перезапустить или остановить.
{/note}
{/ifdef}

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Выполните нужную операцию:

   {tabs}
   
   {tab(Запустить инстанс)}
      
   Выполните одно из действий:

   - Выберите с помощью флажков один или несколько инстансов БД, затем нажмите кнопку **Запустить**.
   - Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста инстанса БД и выберите пункт **Запустить**.

   {/tab}
   
   {tab(Перезапустить инстанс)}
   
   Выполните одно из действий:

   - Выберите с помощью флажков один или несколько инстансов БД, затем нажмите кнопку **Перезапустить**.
   - Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста инстанса БД и выберите пункт **Перезапустить**.

   {/tab}
   
   {tab(Остановить инстанс)}
   
   Выполните одно из действий:

   - Выберите с помощью флажков один или несколько инстансов БД, затем нажмите кнопку **Остановить**.
   - Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста инстанса БД и выберите пункт **Остановить**.

   {/tab}
   
   {/tabs}

{ifdef(public)}
{/tab}

{/tabs}
{/ifdef}

## {heading(Получение информации об инстансе БД и его хостах)[id=dbaas-redis-get-info]}

Для активного и остановленного инстансов БД доступна разная информация.

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Выполните нужную операцию:

   - Чтобы посмотреть информацию об инстансе БД, нажмите на его имя.

   - Чтобы посмотреть информацию об отдельном хосте инстанса БД, нажмите на имя хоста.

   Откроется страница с информацией.

{ifdef(public)}
{/tab}

{/tabs}
{/ifdef}

## {heading(Создание резервной копии)[id=dbaas-redis-create-backup]}

Операция подробно описана в разделе {linkto(../../../../../storage/backups/instructions/create-backup-copy#backup-copy-create)[text=Создание резервной копии вручную]}.

## {heading(Вертикальное масштабирование)[id=dbaas-redis-vertical-scaling]}

В конфигурации **Single** вы можете изменять тип виртуальной машины, который используется хостом инстанса БД.

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста инстанса БД и выберите пункт **Вертикальное масштабирование**.
1. Выберите нужную категорию и тип виртуальной машины.
1. Нажмите кнопку **Сохранить размер**.

{ifdef(public)}
{/tab}

{/tabs}
{/ifdef}

## {heading(Увеличение размера диска)[id=dbaas-redis-increase-disk]}

{note:info}
Размер диска нельзя уменьшить.
{/note}

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Увеличьте размер диска, на котором хранятся данные:
   
   {ifdef(public)}
   {tabs}
   
   {tab(Конфигурация Single)}
   {/ifdef}
      
   1. Выполните одно из действий:

      - Нажмите ![](../../../../../assets/more-icon.svg "inline") для нужного инстанса БД и выберите пункт **Изменить размер диска**.
      - Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста нужного инстанса БД и выберите пункт **Изменить размер диска**.

   1. Укажите новый размер диска.
   1. Нажмите кнопку **Сохранить**.

{ifdef(public)}
   {/tab}
   
   {tab(Конфигурация Кластер)}
   
   {note:info}
   В этой конфигурации размер диска изменяется для всех хостов, входящих в шард.
   {/note}

   1. Нажмите ![](../../../../../assets/more-icon.svg "inline") для нужного шарда инстанса БД и выберите пункт **Изменить размер диска**.
   1. Укажите новый размер диска.
   1. Нажмите кнопку **Подтвердить**.

   {/tab}
   
   {/tabs}

{/tab}

{/tabs}
{/ifdef}

## {heading(Настройка автомасштабирования размера диска)[id=dbaas-redis-disk-autoscaling]}

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Настройте автомасштабирование размера диска, на котором хранятся данные:

   {ifdef(public)}
   {tabs}

   {tab(Конфигурация Single)}
   {/ifdef}

   1. Выполните одно из действий:

      - Нажмите ![](../../../../../assets/more-icon.svg "inline") для нужного инстанса БД и выберите пункт **Изменить автомасштабирование дисков**.
      - Нажмите ![](../../../../../assets/more-icon.svg "inline") для хоста нужного инстанса БД и выберите пункт **Изменить автомасштабирование дисков**.

   1. Включите или выключите опцию автомасштабирования дисков.
   1. Если опция автомасштабирования включена, задайте максимальный размер диска.
   1. Нажмите кнопку **Подтвердить**.

   {ifdef(public)}
   {/tab}

   {/tabs}
   {/ifdef}

{ifdef(public)}
{/tab}

{/tabs}
{/ifdef}

## {heading(Удаление инстанса БД)[id=dbaas-redis-delete-instance]}

{ifdef(public)}
{tabs}

{tab(Личный кабинет)}
{/ifdef}

1. {ifdef(public)}[Перейдите](https://msk.cloud.vk.com/app/){/ifdef}{ifndef(public)}{linkto(../../../../../tools-for-using-services/account/instructions/lk-entry#tools-account-lk-entry)[text=Перейдите]}{/ifndef} в личный кабинет {var(cloud)}.
1. Выберите проект, где находится нужный инстанс БД.
1. Перейдите в раздел **Базы данных** → **Инстансы баз данных**.
1. Чтобы удалить инстанс БД:

   {ifdef(public)}
   {tabs}

   {tab(Конфигурация Single)}
   {/ifdef}

   1. Нажмите ![](../../../../../assets/more-icon.svg "inline") для нужного инстанса БД и выберите пункт **Удалить**.
   1. Нажмите кнопку **Подтвердить**.

{ifdef(public)}
   {/tab}

   {tab(Конфигурация Кластер)}

   1. Нажмите ![](../../../../../assets/more-icon.svg "inline") для нужного инстанса БД и выберите пункт **Удалить кластер**.
   1. Нажмите кнопку **Подтвердить**.

   {/tab}

   {/tabs}

{/tab}

{/tabs}
{/ifdef}