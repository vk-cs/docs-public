1. {linkto(../../connect/kubectl#k8s-kubectl)[text=Подключитесь к кластеру]} с помощью `kubectl`.
1. Выведите события пода, в котором наблюдается проблема, с помощью команды:

   ```console
   kubectl describe pod <ИМЯ_ПОДА> -n <ПРОСТРАНСТВО_ИМЕН> | sed -n '/Events/,$p'
   ```
1. Проверьте, есть ли в разделе `Events` похожие записи: