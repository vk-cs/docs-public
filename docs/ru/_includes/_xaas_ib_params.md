{includetag(service.yaml-common-param)}
[cols="2,4,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`id`
|Идентификатор сервиса UUID4 (ID), сформированный с помощью генератора UUID4
|string (UUID4)
| ![](/ru/assets/check.svg "inline")

|`revision`
|Ревизия сервиса. Сочетание ревизии и ID сервиса определяет его уникальность в Marketplace. Остальные параметры описывают характеристики конкретной ревизии сервиса
|string, до 255 символов
| ![](/ru/assets/check.svg "inline")

|`name`
|Имя сервиса
|string, до 255 символов
| ![](/ru/assets/check.svg "inline")

|`short_description`
|Краткое описание сервиса, которое будет отображаться в его карточке:

* в каталоге Marketplace;
* на вкладке **Описание сервиса** на странице сервиса.

Полное описание сервиса содержится в [файле full_description.md](/ru/tools-for-using-services/vendor-account/reference/ib-apps-full-description)
|string, до 120 символов
| ![](/ru/assets/check.svg "inline")

|`singleton`
|Определяет, есть ли ограничение в один инстанс сервиса на один проект облачной платформы
|boolean
| ![](/en/assets/no.svg "inline")

|`auto_bind`
|Определяет, нужно ли после развертывания сервиса автоматически создавать сервисную привязку
|boolean
| ![](/en/assets/no.svg "inline")

|`icon`
|Имя файла с иконкой сервиса, находящегося в директории `images` [сервисного пакета](../../../instructions/manage-ib-apps/ib-add#service_pack_structure)
|string, до 512 символов
| ![](/ru/assets/check.svg "inline")

|`help`
|URL документации сервиса
|string, до 512 символов
| ![](/ru/assets/check.svg "inline")

|`bindable`
|Определяет, можно ли создавать сервисные привязки для этого сервиса. Значение должно быть `true`
|boolean
| ![](/ru/assets/check.svg "inline")

|`plan_updateable`
|Определяет, может ли пользователь переходить с одного тарифного плана на другой без удаления сервиса.

Значение параметра применяется для всех планов сервиса.

Для image-based приложений значение должно быть `false`
|boolean
| ![](/ru/assets/check.svg "inline")

|`deactivatable`
|Определяет, можно ли временно приостановить использование сервиса
|boolean
| ![](/ru/assets/check.svg "inline")

|`bindings_retrievable`
|Определяет, нужно ли повторять попытку создания сервисной привязки в течение определенного времени, если предыдущая попытка не удалась
|boolean
| ![](/ru/assets/check.svg "inline")

|`instances_retrievable`
|Определяет, нужно ли повторять попытку создания инстанса сервиса в течение определенного времени, если предыдущая попытка не удалась
|boolean
| ![](/ru/assets/check.svg "inline")

|`plans`
|Содержит [имена тарифных планов](/ru/tools-for-using-services/vendor-account/reference/ib-apps-reference/ib-apps-param#ib-apps-param-service-plans) приложения
|Массив
| ![](/ru/assets/check.svg "inline")

|`preview`
|Содержит [имена тарифных опций](/ru/tools-for-using-services/vendor-account/reference/ib-apps-reference/ib-apps-param#service_options_for_matrix) для матрицы тарифных планов
|Массив
| ![](/ru/assets/check.svg "inline")
|===

{note:warn}

Сочетание параметров `id` и `revision` должно быть уникальным в рамках Marketplace. Если приложение с такими же идентификатором и ревизией уже существует в Marketplace, то сервисный пакет не будет обновлен.

{/note}

На основании тарифных планов и опций, указанных в массивах `plans` и `preview`, формируется [матрица тарифных планов](/ru/tools-for-using-services/vendor-account/manage-apps/concepts/about#xaas_tariff_matrix).
{/includetag}

{includetag(service.yaml-plans)}
В массиве `plans` с помощью ключей `name` перечисляются имена тарифных планов (`<ИМЯ_ПЛАНА>`), которые будут доступны пользователю Marketplace.

Пример массива `plans`:

```yaml
plans:
    - name: free
    - name: basic
```

В этом примере перечислены тарифные планы `free` и `basic`. Их конфигурации должны быть описаны в директориях `plans/free` и `plans/basic` соответственно. В матрице тарифных планов эти планы будут отображаться с именами, заданными в параметре `description` файлов `plans/free/plan.yaml` и `plans/basic/plan.yaml`.

{note:warn}

Тарифные планы, не указанные в массиве `plans`, не отображаются в [матрице тарифных планов](/ru/tools-for-using-services/vendor-account/manage-apps/concepts/about#xaas_tariff_matrix) в Marketplace.

{/note}
{/includetag}

{includetag(service.yaml-preview)}
Секция `preview` содержит массив `parameters`. В этом массиве с помощью ключей `name` перечисляются имена тарифных опций (`<ИМЯ_ОПЦИИ>`), которые будут доступны пользователю Marketplace. Массив может быть пустым.

Пример секции `preview`:

```yaml
preview:
  parameters:
  - name: groups
  - name: products
```

В этом примере перечислены тарифные опции `groups` и `products`. Они должны быть описаны в директории `parameters` в файлах `groups.yaml` и `products.yaml` соответственно. В матрице тарифных планов опции будут отображаться с именами, заданными в параметре `description` указанных YAML-файлов.

{note:warn}

Тарифные опции, не указанные в секции `preview`, не отображаются в [матрице тарифных планов](/ru/tools-for-using-services/vendor-account/manage-apps/concepts/about#xaas_tariff_matrix) в Marketplace.

{/note}
{/includetag}

{includetag(service.yaml-example)}
```yaml
id: 72b70199-1823-40c8-aa7e-f43a23ddf380
revision: v. 1.0
name: VK Testers
short_description: Программа крауд-тестирования с многотысячным коммьюнити бета-тестировщиков и собственной платформой для работы с данными
singleton: false
auto_bind: true
icon: icon.png
help: http://vk.cc/vktesters_po_faq
bindable: true
plan_updateable: false
deactivatable: false
bindings_retrievable: true
instances_retrievable: true

plans:
- name: free
- name: basic

preview:
  parameters:
  - name: api_requests_daily_limit
  - name: groups
  - name: products
```
{/includetag}

{includetag(full_description-ib-general)}
В разделе указывается следующая информация:

* Краткий обзор сервиса: что представляет собой сервис, какие задачи помогает решать (основные сценарии использования).
* Кто является поставщиком:

  * Вендор.
  * Дистрибьютор.
  * VK.

   {note:info}

   Для opensource-решений поставщиком является VK.

   {/note}
* Информация о поставщике (опционально).

   {note:info}

   Для opensource-решений информация о поставщике не указывается.

   {/note}
* Характеристики сервиса, желательно в форме таблицы.

  * Тип сервиса: Image-based.

  * Версия сервиса (при наличии). Например, Usergate 7.01.

      {note:info}

      Версия сервиса не является ревизией сервиса, указанной в его конфигурации.

      {/note}
  * Версия ОС. Например, Ubuntu 22.04.
  * Категория сервиса. Примеры:

    * Аналитика.
    * Базы данных.
    * Безопасность.
    * Инструменты для разработки.
    * VPN.

* Варианты развертывания сервиса:

  * Одна ВМ.
  * Кластер.

* Какая инфраструктура облачной платформы используется. Например, ВМ, виртуальные сети, DNS.
* Требования к сети:

  * ВМ должна иметь доступ во внешнюю сеть.
  * В настройках подсети приватный DNS должен быть выключен.

* (Опционально) Схемы сервиса и скриншоты интерфейса.

{cut(Пример заполнения раздела с информацией о сервисе (формат Markdown))}

```markdown
UserGate NGFW содержит все функции решений класса UTM (Unified Threat Management), обеспечивая безопасность от всевозможных интернет-угроз и управление интернет-доступом. UserGate NGFW осуществляет защиту от атак, управление трафиком, аутентификацию интернет-пользователей, а также обеспечивает безопасность посещения сотрудниками интернет-ресурсов, ограждает их от загрузки опасного и вредоносного содержимого.

Поставщик сервиса: компания UserGate.

## Решаемые задачи

### Как межсетевой экран

* Разграничение внешнего интернет-пространства от внутренней или корпоративной сети на физической границе.
* VPN-шлюз для организации доступа и аутентификации конкретных удаленных пользователей к конкретным информационным системам.
* Организация гибридной среды типа локальная инфраструктура-облако посредством Site-to-Site VPN L2TP IPSec и объединения филиалов с центром и облаком VK Cloud.

## Характеристики сервиса:
|                      |                  |
|----------------------|------------------|
| Тип сервиса          |       Image-based|
| ОС                   |              UGOS|
| Версия сервиса       |     Usergate 7.01|
| Категория сервиса    |      Безопасность|

> Image-based — тип сервиса, который развертывается на основе образов виртуальных машин на инфраструктуре VK Cloud.
<br>Для функционирования данного продукта используются:
> * Виртуальная машина.
> * Виртуальные сети.
> * Floating IP-адреса.

**Минимальные требования к инфраструктуре**
Виртуальные машины:
* vCPU: 2.
* RAM: 8 ГБ.
* Размер дисков: не менее 100 ГБ.
* Сеть:
    * ВМ должна иметь доступ во внешнюю сеть.
    * В настройках подсети приватный DNS должен быть **выключен**.
```

{/cut}
{/includetag}

{includetag(full_description-saas-general)}
В разделе указывается следующая информация:

* Краткий обзор сервиса: что представляет собой сервис, какие задачи помогает решать (основные сценарии использования).
* Кто является поставщиком:

  * Вендор.
  * Дистрибьютор.
  * VK.

   {note:info}

   Для opensource-решений поставщиком является VK.

   {/note}
* Информация о поставщике (опционально).

   {note:info}

   Для opensource-решений информация о поставщике не указывается.

   {/note}
* Характеристики сервиса, желательно в форме таблицы.

  * Тип сервиса: SaaS.

  * Версия сервиса (при наличии). Например, Usergate 7.01.

      {note:info}

      Версия сервиса не является ревизией сервиса, указанной в его конфигурации.

      {/note}
  * Категория сервиса. Примеры:

    * Аналитика.
    * Базы данных.
    * Безопасность.
    * Инструменты для разработки.
    * VPN.

* (Опционально) Схемы сервиса и скриншоты интерфейса.

{cut(Пример заполнения раздела с информацией о сервисе (формат Markdown))}

```markdown
UserGate NGFW содержит все функции решений класса UTM (Unified Threat Management), обеспечивая безопасность от всевозможных интернет-угроз и управление интернет-доступом. UserGate NGFW осуществляет защиту от атак, управление трафиком, аутентификацию интернет-пользователей, а также обеспечивает безопасность посещения сотрудниками интернет-ресурсов, ограждает их от загрузки опасного и вредоносного содержимого.

Поставщик сервиса: компания UserGate.

## Решаемые задачи

### Как межсетевой экран

* Разграничение внешнего интернет-пространства от внутренней или корпоративной сети на физической границе.
* VPN-шлюз для организации доступа и аутентификации конкретных удаленных пользователей к конкретным информационным системам.
* Организация гибридной среды типа локальная инфраструктура-облако посредством Site-to-Site VPN L2TP IPSec и объединения филиалов с центром и облаком VK Cloud.

## Характеристики сервиса:
|                      |                  |
|----------------------|------------------|
| Тип сервиса          |       Image-based|
| ОС                   |              UGOS|
| Версия сервиса       |     Usergate 7.01|
| Категория сервиса    |      Безопасность|

> Image-based — тип сервиса, который развертывается на основе образов виртуальных машин на инфраструктуре VK Cloud.
<br>Для функционирования данного продукта используются:
> * Виртуальная машина.
> * Виртуальные сети.
> * Floating IP-адреса.

**Минимальные требования к инфраструктуре**
Виртуальные машины:
* vCPU: 2.
* RAM: 8 ГБ.
* Размер дисков: не менее 100 ГБ.
* Сеть:
    * ВМ должна иметь доступ во внешнюю сеть.
    * В настройках подсети приватный DNS должен быть **выключен**.
```

{/cut}
{/includetag}

{includetag(full_description-ib-tariff)}
Правила тарификации описываются в свободной форме. Рекомендуется:

* Указать [тип тарификации](/ru/tools-for-using-services/vendor-account/manage-apps/concepts/about#xaas_billing).
* Указать, где можно ознакомиться с тарифными планами, если их несколько. Пример: «С правилами тарификации можно ознакомиться в матрице тарифных планов на странице сервиса».
* Упомянуть, что платные тарифные опции, если они есть, тарифицируются дополнительно к стоимости тарифного плана.
* Описать тарифные планы с договорной ценой, если они есть.
* Объяснить, что стоимость инфраструктуры облачной платформы тарифицируется отдельно.

{note:warn}

Если image-based приложение использует инфраструктуру облачной платформы, объем и стоимость которой невозможно определить на момент подключения сервиса (например, хранение накапливаемой по мере работы информации — бекапы), необходимо сообщить об этом в описании правил тарификации.

{/note}

Пример заполнения подраздела с правилами тарификации (формат Markdown):

```markdown
# Правила тарификации
Сервис предоставляется по модели Bring Your Own License. Тарификация осуществляется только за потребляемую инфраструктуру VK Cloud. Ее стоимость можно оценить в блоке **Детализация** в мастере конфигурации тарифного плана. Итоговая стоимость будет отображена на последней странице мастера конфигурации тарифного плана.
```
{/includetag}

{includetag(full_description-saas-tariff)}
Правила тарификации описываются в свободной форме. Рекомендуется:

* Указать [тип тарификации](/ru/tools-for-using-services/vendor-account/manage-apps/concepts/about#xaas_billing).
* Указать, где можно ознакомиться с тарифными планами, если их несколько. Пример: «С правилами тарификации можно ознакомиться в матрице тарифных планов на странице сервиса».
* Упомянуть, что платные тарифные опции, если они есть, тарифицируются дополнительно к стоимости тарифного плана.
* Описать тарифные планы с договорной ценой, если они есть.

Пример заполнения подраздела с правилами тарификации (формат Markdown):

```markdown
# Правила тарификации
Сервис предоставляется по модели Bring Your Own License. Тарификация осуществляется только за потребляемую инфраструктуру VK Cloud. Ее стоимость можно оценить в блоке **Детализация** в мастере конфигурации тарифного плана. Итоговая стоимость будет отображена на последней странице мастера конфигурации тарифного плана.
```
{/includetag}

{includetag(full_description-links)}
* Ссылка на краткую документацию, описывающую начало работы с сервисом — ссылка на статью [Настройка сервисов магазина приложений](/ru/applications-and-services/marketplace/initial-configuration).
* Ссылка на полную документацию, опубликованную разработчиком сервиса.

Пример заполнения раздела с полезными ссылками (формат Markdown):

```markdown
# Полезные ссылки
- [Документация c первыми шагами](<ССЫЛКА_НА_ПОРТАЛ_ДОКУМЕНТАЦИИ_VK_CLOUD> "На портале документации VK Cloud") — инструкции по получению доступа, первоначальной настройке и началу использования сервиса после развертывания инстанса.
- [Полная документация сервиса](<ССЫЛКА_НА_ДОКУМЕНТАЦИЮ_РАЗРАБОТЧИКА> "На сайте поставщика").
```
{/includetag}

{includetag(full_description-ib-support)}
В разделе приводятся:

* Правила получения технической поддержки сервиса.
* Правила получения технической поддержки по инфраструктуре облачной платформы.
* Контакты технической поддержки.

Пример заполнения раздела о технической поддержке (формат Markdown):

```markdown
# Техническая поддержка
Услуга по технической поддержке UserGate NGFW приобретается вместе с лицензией на продукт. Поддержка осуществляется поставщиком на [Портале технической поддержки](https://support.usergate.com/ru) в соответствии с [Регламентом технической поддержки](https://support.usergate.com/ru/technical-support-regulations).
По запросу [команда Professional Services](https://cloud.vk.com/professional-services/) VK Cloud осуществляет расширенную техническую поддержку UserGate NGFW.

Команда VK Cloud осуществляет техническую поддержку по вопросам функционирования инфраструктуры VK Cloud. Информация об условиях оказания техподдержки и способах обращения находится на странице [Условия оказания поддержки](/ru/start/support/support-info).
```
{/includetag}

{includetag(full_description-saas-support)}
В разделе приводятся:

* Правила получения технической поддержки сервиса.
* Контакты технической поддержки.

Пример заполнения раздела о технической поддержке (формат Markdown):

```markdown
# Техническая поддержка
Услуга по технической поддержке UserGate NGFW приобретается вместе с лицензией на продукт. Поддержка осуществляется поставщиком на [Портале технической поддержки](https://support.usergate.com/ru) в соответствии с [Регламентом технической поддержки](https://support.usergate.com/ru/technical-support-regulations).
По запросу [команда Professional Services](https://cloud.vk.com/professional-services/) VK Cloud осуществляет расширенную техническую поддержку UserGate NGFW.

Команда VK Cloud осуществляет техническую поддержку по вопросам функционирования инфраструктуры VK Cloud. Информация об условиях оказания техподдержки и способах обращения находится на странице [Условия оказания поддержки](/ru/start/support/support-info).
```
{/includetag}

{includetag(full_description-legal)}
В разделе указываются:

* Ссылки:

  * На [Пользовательское соглашение магазина приложений](/ru/start/legal/digital-cloud/marketplace)
  * На пользовательское соглашение сервиса.

* Для Open Source-решений — дисклеймер о лицензиях Open Source.

Пример заполнения раздела о лицензионном соглашении (формат Markdown):

```markdown
# Лицензионное соглашение
Используя сервис UserGate NGFW, вы соглашаетесь с:
- [Пользовательским соглашением магазина приложений](https://cloud.vk.com/docs/ru/start/legal/digital-cloud/marketplace).
- [Лицензионным соглашением на использование UserGate NGFW](https://www.usergate.com/ru/usergate-eula).
```
{/includetag}

{includetag(plan.yaml-common-param)}

[cols="2,5,2,2", options="header"]
|===
| Имя
| Описание
| Формат
| Обязательный

| `id`
| Идентификатор тарифного плана UUID4 (ID), сформированный с помощью генератора UUID4
| `string (UUID4)`
| ![](/ru/assets/check.svg "inline")

| `revision`
| Ревизия тарифного плана. Сочетание ревизии и ID тарифного плана определяет его уникальность в сервисе. Остальные параметры описывают характеристики конкретной ревизии тарифного плана.

{note:warn}

Если тарифный план с такими же идентификатором и ревизией уже существует в этом сервисе, план не будет обновлен

{/note}
| `string`, до 255 символов
| ![](/ru/assets/check.svg "inline")

| `name`
| Техническое имя тарифного плана, которое не отображается в интерфейсе Marketplace. Может содержать только латинские буквы и символ `_`
| `string`, до 255 символов
| ![](/ru/assets/check.svg "inline")

| `description`
| Имя тарифного плана, которое отображается в интерфейсе Marketplace
| `string`, до 255 символов
| ![](/ru/assets/check.svg "inline")

| `free`
| Определяет, является ли тарифный план бесплатным
| `boolean`
| ![](/ru/assets/check.svg "inline")

| `billing`
| [Секция billing тарифного плана](#plan_billing), определяет стоимость тарифного плана без учета платных тарифных опций
| ![](/ru/assets/no.svg "inline")
| ![](/ru/assets/check.svg "inline")

| `parameters_patch`
| [Секция parameters_patch](#plan_options), выборочно переопределяет параметры тарифных опций для конкретного плана
| ![](/ru/assets/no.svg "inline")
| ![](/ru/assets/no.svg "inline")

| `resource_usages`
| Список постоплатных тарифных опций.

{note:warn}

Нельзя указать для тарифного плана с предоплатными тарифными опциями

{/note}
| Массив
| Да — для плана с постоплатными опциями
|===

{/includetag}

{includetag(plan.yaml-billing)}
Параметры секции:

[cols="3,5,2,1,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный
|Значение по умолчанию

|`cost`
|Стоимость тарифного плана за отчетный период без учета платных тарифных опций. Задается в валюте страны, где развернут Marketplace.

Для бесплатного тарифного плана — `0`
|`float`
| ![](/ru/assets/check.svg "inline")
| —

|`refundable`
|Определяет, возвращать или нет на бонусный счет проекта денежные средства за оставшиеся дни расчетного периода, если пользователь изменил тарифный план или удалил инстанс сервиса.

Параметр влияет на дату списания оплаты за сервис, когда пользователь изменяет тарифный план (редактирует тарифные опции или переходит на новый):

- Если `true`, дата не изменяется.
- Если `false`, дата обновляется до даты изменения тарифного плана

|`boolean`
| ![](/ru/assets/no.svg "inline")
|`true`

|`billing_cycle_flat`
|Длительность отчетного периода для тарификации.

Формат записи: `<КОЛИЧЕСТВО_МЕСЯЦЕВ> mons <КОЛИЧЕСТВО_ДНЕЙ> days`. Например, `1 mons 15 days`, `30 days`.

{note:info}

Количество дней в месяце `mons` рассчитывается на основе календарного значения, поэтому периоды `1 mons 0 days` и `0 mons 31 days` не равны между собой.

{/note}
|`string`
| ![](/ru/assets/no.svg "inline")
|`1 mons 0 days`
|===

Пример заполнения секции `billing`:

```yaml
billing:
  cost: 2000
  refundable: true
  billing_cycle_flat: 1 mons 0 days
```

{/includetag}

{includetag(plan.yaml-parameters_patch)}
В секции выборочно переопределяются параметры тарифных опций для тарифного плана: отдельные параметры или целые секции описания тарифной опции.

{tabs}
{tab(Переопределение отдельных параметров тарифных опций)}

Для каждого переопределяемого параметра указывается его имя с путем до корневой секции. Имя параметра и имена его родительских секций разделяются точками.

Пример переопределения параметров тарифных опций:

```yaml
parameters_patch:
  users:
    schema.const: 5000
  volume_data_size:
    schema.default: 550
    schema.minimum: 550
```

В этом примере новые значения получают:

- параметр `cost` в секции `schema` тарифной опции `users`;
- параметры `default` и `minimum` в секции `schema` тарифной опции `volume_data_size`.

Возможные параметры в зависимости от типа тарифной опции приведены в разделе [YAML-файл тарифной опции](/ru/tools-for-using-services/vendor-account/reference/ib-apps-reference/tariff-options).

{/tab}
{tab(Переопределение секции тарифной опции целиком)}

Переопределяемая секция тарифной опции указывается в том же формате, что и в YAML-файле тарифной опции.

Пример переопределения секции `billing` тарифной опции:

```yaml
parameters_patch:
  assemblies_size:
    billing:
      base: 25
      cost: 0
      unit:
        size: 100
```

В этом примере секция `billing` тарифной опции `assemblies_size` целиком заменяется на новую.

{/tab}
{/tabs}

{note:warn}
Одновременное переопределение секции целиком и отдельного параметра из этой секции запрещено.
{/note}

Значения параметров, указанные в секции `parameters_patch`, имеют приоритет над значениями из YAML-файлов тарифных опций.

Если в тарифных планах используются опции, которые сильно отличаются друг от друга:

- Рекомендуется описать каждую опцию отдельным YAML-файлом в директории `parameters`.
- Не рекомендуется описывать одну тарифную опцию для всех тарифных планов и переопределять большинство ее параметров в разных тарифных планах.

{/includetag}

{includetag(plan.yaml-example)}
```yaml
id: b2b42648-XXXX-b4cddbf010b2
revision: v. 1.0
name: basic
description: Базовый
free: false

billing:
  cost: 2000
  refundable: true
  billing_cycle_flat: 1 mons

parameters_patch:
  users:
    schema.const: 5000
  volume_data_size:
    schema.default: 550
    schema.minimum: 550
```
{/includetag}

{includetag(plan.yaml-example-free-postpaid)}
```yaml
id: 3aa541d8-XXXX-6dbf542b3f90
revision: v. 1.1
name: postpaid
description: Постоплатный
free: true

billing:
  cost: 0

resource_usages:
  - storage
```
{/includetag}

{includetag(display.yaml-pages)}

Массив описывает страницы мастера конфигурации для тарифного плана. На каждой странице отображается одна или несколько групп тарифных опций. Максимальное количество страниц — 5.

{note:warn}

Каждая тарифная опция плана должна входить в одну и только одну группу.

{/note}

Параметры массива `pages`:

[cols="2,5,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`name`
|Имя страницы мастера конфигурации, не более 32 символов
|`string`
|![](/ru/assets/check.svg "inline")

|`groups`
|Группы тарифных опций, отображаемые на конкретной странице мастера конфигурации
|Массив
|![](/ru/assets/check.svg "inline")

|`groups.name`
|Имя группы тарифных опций, не более 255 символов
|`string`
|![](/ru/assets/check.svg "inline")

|`groups.parameters`
|Список имен тарифных опций, входящих в группу.
|Список, элемент списка — `string`
|![](/ru/assets/check.svg "inline")

|`groups.parameters.name`
|Имя тарифной опции, совпадающее с именем YAML-файла этой опции: `<ИМЯ_ОПЦИИ>`.
|`string`
|![](/ru/assets/check.svg "inline")
|===

По умолчанию в мастере конфигурации тарифного плана отображаются все группы тарифных опций (`groups`), указанные в массиве `pages`. Для отображения или скрытия групп в зависимости от заданных условий используется [конструкция when](#when_construction).

{/includetag}

{includetag(display.yaml-entities-common)}

Массив описывает элементы инфраструктуры VK Cloud, которые будут тарифицироваться в рамках тарифного плана.

{note:warn}

[Манифесты Terraform](/ru/tools-for-using-services/vendor-account/reference/ib-apps-reference/tf-manifest) должны содержать описание элементов инфраструктуры из `entities`.

{/note}

По умолчанию в мастере конфигурации тарифного плана указывается стоимость всех элементов инфраструктуры, описанных в массиве `entities`. Для отображения или скрытия стоимости инфраструктуры в зависимости от заданных условий используется [конструкция when](#when_construction).

Общие параметры массива `entities`:

[cols="2,5,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`entity`
|Тип элемента инфраструктуры:

* `vm` — ВМ.
* `load_balancing` — балансировщик нагрузки.
* `floating_ip` — внешний IP-адрес.
|`string`
|![](/ru/assets/check.svg "inline")

|`description`
|Описание элемента инфраструктуры
|`string`
|![](/ru/assets/no.svg "inline")
|===

Пример описания страницы **Настройки сервиса** в мастере конфигурации тарифного плана:

```yaml
pages:
- name: Настройки сервиса # Имя страницы
  groups:
  - name: Сервер # Имя группы тарифных опций
    parameters:
    - name: network # Имя тарифной опции
    - name: vm
    - name: az

  - name: Системный диск
    parameters:
    - name: volume_size
    - name: volume_type
```

{/includetag}

{includetag(display.yaml-entities-vm)}

Параметры массива `entities` для ВМ:

[cols="2,5,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`count`
|Количество ВМ, задается параметром `const` или `param`
|Секция
|![](/ru/assets/check.svg "inline")

|`count.const`
|Количество ВМ
|`integer`
| —

|`count.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), значение которой определяет количество ВМ
|`string`
| —

|`flavor`
|Тип ВМ, задается параметром `const` или `param`
|Секция
|![](/ru/assets/check.svg "inline")

|`flavor.const`
|[ID шаблона конфигурации ВМ](/ru/tools-for-using-services/cli/openstack-cli#openstack-example)
|`string`
| —

|`flavor.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), описывающей тип ВМ: `datasource.type` = `flavor`
|`string`
| —

|`disks`
|Диски ВМ. Каждый диск описывается секциями `type` и `size`
|Массив
|![](/ru/assets/check.svg "inline"), если в массиве `pages` указана опция типа `datasource.type` = `volume_type`

|`disks.type`
|Тип диска, задается параметром `const` или `param`
|Секция
| —

|`disks.type.const`
|Тип диска.

Возможные значения:

* `ceph-ssd` — диск типа SSD.
* `ceph-hdd` — диск типа HDD.
* `high-iops` — диск типа High-IOPS SSD (SSD с повышенной производительностью).
|`string`
| —

|`disks.type.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), описывающей тип диска: `datasource.type` = `volume_type`
|`string`
| —

|`disks.size`
|Размер диска, задается параметром `const` или `param`
|Секция
| —

|`disks.size.const`
|Размер диска
|`integer`
| —

|`disks.size.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), значение которой определяет размер диска
|`string`
| —
|===

{note:warn}

Массив `disks` в описании ВМ является обязательным, если в массиве `pages` тарифного плана используется тарифная опция типа `datasource`, описывающая тип диска.

{/note}

Пример описания ВМ для мастера конфигурации тарифного плана:

```yaml
entities:
  - entity: vm
    description: Виртуальная машина
    count:
      const: 1
    flavor:
      param: ds-flavor # Имя тарифной опции
    disks:
      - type:
          param: root_type
        size:
          param: root_size
      - type:
          param: data_type
        size:
          param: data_size
```

{/includetag}

{includetag(display.yaml-entities-lb)}

Параметры массива `entities` для балансировщика нагрузки:

[cols="2,5,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`count`
|Количество балансировщиков нагрузки, задается параметром `const` или `param`
|Секция
|![](/ru/assets/check.svg "inline")

|`count.const`
|Количество балансировщиков
|`integer`
| —

|`count.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), значение которой определяет количество балансировщиков
|`string`
| —
|===

Пример описания балансировщика нагрузки для мастера конфигурации тарифного плана:

```yaml
entities:
  - entity: load_balancing
    count:
      param: number_balancing # Имя тарифной опции
```

{/includetag}

{includetag(display.yaml-entities-ip)}

Параметры массива `entities` для внешнего IP-адреса:

[cols="2,5,2,2", options="header"]
|===
|Имя
|Описание
|Формат
|Обязательный

|`count`
|Количество внешних IP-адресов, задается параметром `const` или `param`
|Секция
|![](/ru/assets/check.svg "inline")

|`count.const`
|Количество внешних IP-адресов
|`integer`
| —

|`count.param`
|Имя тарифной опции (`<ИМЯ_ОПЦИИ>`), значение которой определяет количество внешних IP-адресов
|`string`
| —
|===

Пример описания внешнего IP-адреса для мастера конфигурации тарифного плана:

```yaml
entities:
  - entity: floating_ip
    count:
      const: 1
```

{/includetag}

{includetag(display.yaml-when-common)}

Конструкция `when` управляет условным ветвлением при отображении мастера тарифного плана. Она содержит:

* Тип условия:

  * `when.in` — проверка равенства значений, заданных параметрами `key` и `values`. Условие выполнено, если значение `key` равно хотя бы одному из значений `values`.
  * `when.not_in` — проверка неравенства значений, заданных параметрами `key` и `values`. Условие выполнено, если значение `key` не равно ни одному из значений `values`.

* Критерии. Задаются параметрами `key` и `values` одним из способов:

  * Через параметр `param` — чтобы использовать значение конкретной тарифной опции.
  * Через параметр `const` — чтобы использовать фиксированное значение.

  В `key` можно задать одно значение, в `values` — одно или несколько.

Структура `when` в формате YAML:

```yaml
when:
  in: # или not_in
    key:
      param: <ИМЯ_ОПЦИИ> # или const: <ЗНАЧЕНИЕ>
    values:
      - const: <ЗНАЧЕНИЕ>
      - param: <ИМЯ_ОПЦИИ>
      ...
```
Здесь:

* `<ИМЯ_ОПЦИИ>` — имя тарифной опции, совпадающее с именем YAML-файла этой опции.
* `<ЗНАЧЕНИЕ>` — фиксированное значение.

{note:warn}

Ресурсы в [манифестах Terraform](/ru/tools-for-using-services/vendor-account/reference/ib-apps-reference/tf-manifest) должны быть описаны с учетом условий, заданных в конструкциях `when`.

{/note}

{/includetag}

{includetag(display.yaml-when-pages)}

В массиве `pages` конструкция `when` используется, чтобы задать условие, которое определяет, нужно ли в мастере конфигурации тарифного плана отображать группу тарифных опций.

{note:warn}

Поддерживается только первый уровень иерархии в условиях отображения групп. Если в группе опций указана конструкция `when`, тарифные опции из этой группы, перечисленные в массиве `parameters`, нельзя использовать в конструкции `when` в других группах.

{/note}

Одна и та же тарифная опция может использоваться в конструкциях `when` в разных группах. В зависимости от значения тарифной опции в мастере конфигурации будут отображаться те или иные группы.

При использовании конструкции `when` страница с группами тарифных опций будет скрыта, если одновременно выполняются следующие условия:

* В рамках одной страницы все группы имеют конструкцию `when`.
* В мастере конфигурации тарифного плана заданы такие значения тарифных опций, что условия во всех группах на этой странице не выполняются.

Пример использования конструкции `when` в массиве `pages`:

```yaml
pages:
- name: Настройки бекапа # Имя страницы
  groups:
  - name: High-frequency бекап # Имя группы тарифных опций
    parameters:
    - name: frequency_per_day # Имя тарифной опции
    when:
      in:
        key:
          param: backup_method # Имя тарифной опции
        values:
          - const: high-frequency
```

В этом примере группа `High-frequency бекап` с тарифной опцией `frequency_per_day` отобразится, если в мастере конфигурации пользователь выбрал значение `high-frequency` для тарифной опции `backup_method`. Далее пользователь сможет выбрать частоту создания бекапов.

Тарифная опция `backup_method`:

* может использоваться в конструкции `when` в других группах;
* должна быть указана в массиве `parameters` в другой группе.

Тарифная опция `frequency_per_day` не может использоваться в других группах:

* в конструкции `when` — потому что поддерживается только один уровень иерархии условий;
* в `parameters` — потому что одна тарифная опция может быть указана только в одной группе.

{/includetag}

{includetag(display.yaml-when-entities)}

В массиве `entities` конструкция `when` используется, чтобы задать условие, которое определяет, нужно ли в мастере конфигурации тарифного плана отображать стоимость элемента инфраструктуры, описанного в массиве `entities`.

Пример использования конструкции `when` в массиве `entities`:

```yaml
entities:
  - entity: vm
    description: alkair_software
    when:
      in:
        key:
          param: need_install_alkair_software # Имя тарифной опции
        values:
          - const: true
    flavor:
      const: 6a7a0690-943a-4921-936e-2849970ccfba # Тип ВМ (CPU, RAM). В этом примере ВМ с 2 CPU и 3 ГБ RAM
    count:
      const: 1
    disks:
      - type:
          const: high-iops # Тип диска
        size:
          const: 500 # Размер диска
```

В этом примере приведена настройка отображения стоимости ВМ для ПО Алькаир. Если пользователь выберет для тарифной опции `need_install_alkair_software` значение `true`, в мастере конфигурации тарифного плана будет отображена стоимость ВМ, описанной в элементе `entities.entity`.

{/includetag}