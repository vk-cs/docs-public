# {heading(Аутентификация и авторизация)[id=mllib-lib-authz]}

{include(../../../_includes/_mlplatform_library_authz.md)[tags=intro_part]}

В [справочнике методов библиотеки](../lib-reference) указывается, какая роль должна быть у токена, чтобы вызвать конкретный метод.

{include(../../../_includes/_mlplatform_library_authz.md)[tags=main_part]}
