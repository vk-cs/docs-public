# {heading(Управление пулом рабочих столов)[id=desktops-pool]}

{ifdef(public)}
Пул рабочих столов — это объединение нескольких рабочих столов, которые используют общие вычислительные ресурсы. Рабочие столы не могут быть созданы без пула.
{/ifdef}