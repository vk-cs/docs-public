# {heading(Подключение к OpenStack CLI)[id=tools-cli-openstack]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-intro]}

{ifdef(public)}
## {heading(Подготовительные шаги)[id=openstack-prepare]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-prepare]}
{/ifdef}

## {heading({counter(cli)}. Установите клиент OpenStack)[id=openstack-install]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install]}

## {heading({counter(cli)}. {ifdef(public)}(Опционально) {/ifdef}Установите дополнительные пакеты)[id=openstack-install-package]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install-package]}

## {heading({counter(cli)}. Пройдите аутентификацию)[id=openstack-authorize]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-authorize]}

## {heading({counter(cli)}. Проверьте готовность OpenStack CLI к работе)[id=openstack-check]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-check]}

## {heading(Примеры команд OpenStack CLI)[id=openstack-example]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-example]}