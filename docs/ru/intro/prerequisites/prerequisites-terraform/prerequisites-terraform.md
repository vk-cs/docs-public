# {heading(Установка и инициализация Terraform)[id=terraform-install]}

## {heading(Установка Terraform)[id=terraform-install-prepare]}

{include(../../../_includes/_terraform-install.md)[tags=terraform-install-prepare,prerequisites-terraform-install-prepare]}

## {heading(Инициализация Terraform)[id=terraform-install-init]}

{include(../../../_includes/_terraform-install.md)[tags=terraform-install-init]}

## Что дальше?

1. Обратитесь к администратору, чтобы получить {linkto(../../../tools-for-using-services/terraform/reference/configuration#terraform-resource-config)[text=файлы конфигурации]} для создания ресурсов.
1. {linkto(../../../tools-for-using-services/terraform/instructions/create-resources#terraform-create-resources)[text=Создайте ресурс]} с помощью Terraform.