# {heading(Подключение к OpenStack CLI)[id=prerequisites-openstack-cli]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-intro]}

## {heading({counter(prerequisites-cli)}. Установите клиент OpenStack)[id=prerequisites-openstack-install]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install]}

## {heading({counter(prerequisites-cli)}. Установите дополнительные пакеты)[id=prerequisites-openstack-install-package]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install-package]}

## {heading({counter(prerequisites-cli)}. Пройдите аутентификацию)[id=prerequisites-openstack-authorize]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-authorize]}

## {heading({counter(prerequisites-cli)}. Проверьте готовность OpenStack CLI к работе)[id=prerequisites-openstack-check]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-check]}

## {heading(Примеры команд OpenStack CLI)[id=prerequisites-openstack-example]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-example]}