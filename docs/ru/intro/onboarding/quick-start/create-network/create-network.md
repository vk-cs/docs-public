# {heading(Cloud Network: создайте виртуальную сеть и настройте группу безопасности)[id=onboarding-create-network]}

[Cloud Network](/ru/networks/vnet) — группа сервисов для управления сетевой связностью в {var(cloud)}. Эти сервисы обеспечивают обмен данными между ресурсами, подключенными к виртуальной сети.

Аналогично физическим локальным сетям, у виртуальной сети есть свои подсети — диапазоны IP-адресов. При подключении к виртуальной сети каждый облачный ресурс получает свой IP-адрес и может обмениваться трафиком как внутри своей подсети, так и между разными подсетями одной сети.

Начните работу с Cloud Network любым удобным способом:

- Воспользуйтесь [инструкцией](#onboarding-create-network-guide), чтобы создать свою виртуальную сеть с несколькими виртуальными машинами и настроить правила прохождения трафика.
- Пройдите [бесплатный обучающий курс](#onboarding-create-network-course), чтобы познакомиться с возможностями сервиса.

## {heading(Создайте и настройте виртуальную сеть с ВМ)[id=onboarding-create-network-guide]}

Пройдя все шаги этой инструкции, вы:

1. {linkto(#includes-create-network-network-create)[text=Создадите]} виртуальную сеть с доступом и без доступа в интернет.
1. {linkto(#includes-create-network-vms-create)[text=Создадите]} виртуальные машины с разными вариантами подключения к сетям.
1. {linkto(#includes-create-network-sg-create)[text=Создадите]} группу безопасности, в которой настроите правила для управления трафиком.
1. {linkto(#includes-create-network-sg-assign)[text=Создадите]} группу безопасности ВМ, чтобы разрешить передачу трафика.
1. {linkto(#includes-create-network-connection-check)[text=Проверите]} сетевую связность между ВМ.
1. {linkto(#includes-create-network-internet-check)[text=Проверите]} доступ в интернет.
1. {linkto(#includes-create-network-ssh-check)[text=Проверите]} подключение к ВМ по SSH.

{include(../../../../_includes/_create-network.md)[level=+1]}

## {heading(Обучающий курс)[id=onboarding-create-network-course]}

Пройдите [бесплатный обучающий курс](https://cloud.vk.com/cloud-native-courses/advanced/virtualnye-seti-i-marshrutizaciya/). 

В теоретической части курса рассказано, как работают виртуальные сети, маршрутизаторы и системы контроля сетевого доступа. В практической части вы создадите свою виртуальную сеть с подсетью, проверите полученную топологию сети в интерактивном редакторе, а также ознакомитесь с маршрутизаторами и группами безопасности в личном кабинете {var(cloud)}.

## {heading(Вопросы и ответы)[id=onboarding-create-network-faq]}

{cut(Какой адрес подсети выбрать?)}

Выбирайте диапазоны IP-адресов для частных сетей:

[cols="1,1", options="header"]
|===
|Диапазон по [RFC 1918](http://www.faqs.org/rfcs/rfc1918.html)
|Пример адреса подсети

|10.0.0.0 - 10.255.255.255 (10.0.0.0/8)
|10.0.2.0/24

|172.16.0.0 - 172.31.255.255 (172.16.0.0/12)
|172.31.0.0/16

|192.168.0.0 - 192.168.255.255 (192.168.0.0/16)
|192.168.199.0/24
|===

Рекомендации:

- Стандартный выбор для новой подсети: 10.0.2.0/24 или 192.168.1.0/24. Маска /24 создает для подсети 254 адреса — этого хватает для большинства задач.
- Не используйте зарезервированные адреса в подсети. Первый адрес подсети резервируется для шлюза, а если для подсети включен DHCP, то в ней размещаются два DHCP-сервера, которые занимают первые два адреса из {linkto(../../../../networks/vnet/concepts/ips-and-inet#vnet-ips-and-inet-network-addressing)[text=пула DHCP-адресов]}.
- Не пересекайте диапазоны адресов с инфраструктурой On-Premise, если планируете организовать VPN-подключение.

{/cut}

{cut(Как связать несколько виртуальных машин между собой?)}

Чтобы подключить одну виртуальную машину {var(cloud)} к другой, обеспечьте сетевую связность между их подсетями.

- Виртуальные машины в одной подсети могут обмениваться трафиком без дополнительных настроек.
- Виртуальные машины в разных подсетях по умолчанию не могут обмениваться трафиком. Сетевую связность нужно настроить с помощью {linkto(../../../../networks/vnet/concepts/router#vnet-router-standard)[text=стандартного маршрутизатора]}.

В обоих случаях {linkto(../../../../networks/vnet/concepts/traffic-limiting#vnet-traffic-limiting-secgroups)[text=группы безопасности]} должны разрешать трафик между виртуальными машинами.

{/cut}

{cut(Как подключить домен?)}

Настройте подходящий вариант:

- [Публичный DNS](/ru/networks/dns/instructions/publicdns) для публичных доменов, доступных из интернета.
- {linkto(../../../../networks/dns/instructions/private-dns#dns-private-dns)[text=Приватный DNS]} для доменов, доступных только внутри {var(cloud)}.

Чтобы A-записи появлялись в DNS-зоне автоматически, при создании виртуальных машин включайте опцию **Привязать домен к внешнему IP**.

{/cut}

{cut(Как работает управление сетями?)}

В {var(cloud)} используется SDN (Software-Defined Networking) собственной разработки — {linkto(../../../../networks/vnet/concepts/sdn#vnet-sdn-sprut)[text=Sprut]}. Она подключается автоматически для всех новых проектов.

Для проектов, созданных до появления {linkto(../../../../networks/vnet/concepts/sdn#vnet-sdn-sprut)[text=Sprut]}, установлен SDN прошлого поколения — {linkto(../../../../networks/vnet/concepts/sdn#vnet-sdn-neutron)[text=Neutron]}. {linkto(../../../../tools-for-using-services/account/instructions/project-settings/manage#project-sdn-view)[text=Проверить]} текущую SDN можно в настройках облачного проекта.

{/cut}

{cut(Как настроить распределение входящего трафика между серверами?)}

Распределение трафика выполняют {linkto(../../../../networks/balancing/concepts/load-balancer#balancing-load-balancer)[text=балансировщики нагрузки]}. Без их использования весь входящий поток данных направляется на один сервер.

{/cut}

{cut(Как организовать VPN-подключение к облачной инфраструктуре {var(cloud)}?)}

Вы можете {linkto(../../../../networks/vnet/instructions/vpn#vnet-vpn)[text=управлять]} VPN-туннелями через личный кабинет или OpenStack CLI.

{linkto(../../../../networks/vnet/concepts/onpremise-connect#vnet-onpremise-connect)[text=Подробнее о вариантах подключения к удаленной инфраструктуре]}.

{/cut}

{cut(Что тарифицируется в сервисе Cloud Network?)}

{linkto(../../../../networks/vnet/tariffication#vnet-tariffication)[text=Тарифицируются]} публичные IP-адреса и балансировщики нагрузки. Удаляйте эти ресурсы, как только они перестанут быть нужными.

{/cut}
