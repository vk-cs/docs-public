# {heading(События компонента Karboii)[id=event-log-karboii]}

События, которые {linkto(../../../../../storage/backups/concepts/about#backup-about)[text=сервис резервного копирования]} передает в Cloud Audit.

В таблицах указано краткое имя ресурса. Полное имя содержит префикс `vkcloud.cloud_backup`.

## {heading(Доменные события)[id=event-log-domain-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=domain-events]}

## {heading(События запросов к API)[id=event-log-api-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=api-events]}