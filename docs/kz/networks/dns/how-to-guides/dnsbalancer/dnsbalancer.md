# {heading(DNS теңестіру)[id=dns-dnsbalancer]}

{include(/kz/_includes/_translated_by_ai.md)}

{var(cloud)} қолданба инфрақұрылымы орналасқан деректер орталығы істен шыққан жағдайда да жұмысын жалғастыратын ақауға төзімді қолданбаларды жасауға мүмкіндік береді.

Ақауға төзімділікті қамтамасыз ету үшін қолданба инфрақұрылымы бір аймақтың бірнеше {linkto(../../../../start/concepts/architecture#az)[text=қолжетімділік аймақтарында]} қайталануы керек, ал аймақтардың бірі қолжетімсіз болған жағдайда балансировка сұрауларды көрші аймақтардағы серверлерге қайта бағыттауы тиіс. Мұндай архитектура үшін қолжетімділік аймақтарының ең аз саны — екі, оңтайлысы — үш.

DNSBalancer сервисі қолжетімділік аймақтары арасында сұрауларды теңестіруді орындайды.

{cut(Сервистің жұмыс істеу қағидаты)}

DNSBalancer сервисі қолданба өрістетілген ВМ-ларға, барлық қолжетімділік аймақтарында орнатылады.

Сервис өзі орнатылған ВМ-ның жария IP-мекенжайының (A-жазбасының) және басқа қолжетімділік аймақтарындағы ВМ-лардың қолжетімділігін тексереді.

Егер сервис өзінің ВМ IP-мекенжайы қолжетімсіз екенін анықтаса, ол жария DNS-тегі сәйкес A-жазбаны жояды.

Егер сервис көрші ВМ-ның IP-мекенжайы қолжетімсіз екенін анықтаса:

1. Сервис өзінің ВМ жария мекенжайымен A-жазбасын жасайды (егер A-жазбасы әлі жасалмаса).
1. Сервис қолжетімсіз жария мекенжайлары бар A-жазбаларды жояды.

Мақсатты сервиске сұраулар тізімдегі тек қолжетімді IP-мекенжайлар арқылы ғана жіберіледі.

Тексеру берілген уақыт аралығы арқылы циклдік түрде орындалады. Егер кейінгі тексеру кезінде ВМ қолжетімді болса, оның IP-мекенжайына сәйкес A-жазба тізімге қайта қосылады.

{/cut}

Мақалада DNSBalancer сервисін әртүрлі қолжетімділік аймақтарындағы үш ВМ-ға қалай орнатып, баптау керектігі көрсетілген. Сервис бапталғаннан кейін қолданбаға сұраулар жұмыс істеп тұрған ВМ-лардың біріне жіберіледі. Егер ВМ қолжетімсіз болса, оған сұраулар жіберілмейді.

## {heading(Дайындық қадамдары)[id=dns-dnsbalancer-prep]}

1. {var(cloud)} жеке кабинетіне [өтіңіз](https://kz.cloud.vk.com/app).
1. Интернетке қолжетімді, әртүрлі {linkto(../../../../start/concepts/architecture#az)[text=қолжетімділік аймақтарында]} орналасқан үш ВМ таңдаңыз немесе жасаңыз.
1. Көрсетілген ВМ-ларға SSH арқылы қосылыңыз.

## {heading(1. Сервисті орнатыңыз)[id=dns-dnsbalancer-download]}

1. ОЖ-ңызға арналған `dnsbalancer` файлын жүктеп алыңыз.

    {tabs}

    {tab(Ubuntu, Debian)}

    Жүктеу файлы: [dnsbalancer](assets/dnsbalancer "download")

    {/tab}

    {tab(FreeBSD)}

    Жүктеу файлы: [dnsbalancer](assets/dnsbalancer_freebsd "download")

    Жүктеп алғаннан кейін файлдың атауын `dnsbalancer` деп өзгертіңіз.

    {/tab}

    {tab(RedOS)}

    Жүктеу файлы: [dnsbalancer](assets/dnsbalancer_redos "download")

    Жүктеп алғаннан кейін файлдың атауын `dnsbalancer` деп өзгертіңіз.

    {/tab}

    {/tabs}

1. Әр ВМ үшін терминал сессиясын ашыңыз.
1. Жүктеп алынған файлды әр ВМ-ның `/usr/local/bin/` директориясына орналастырыңыз.

## {heading(2. Сервис конфигурациясы файлын жасаңыз)[id=dns-dnsbalancer-config]}

1. ВМ-лардың бірінде терминал сессиясын ашыңыз.
1. `/etc/dnsbalancer/config.yaml` конфигурация файлын жасаңыз.
1. Файлда сервисті баптауға арналған негізгі параметрлерді көрсетіңіз:

    ```yaml
    config:
        url: "https://mcs.mail.ru/public-dns/" 
        auth_url: "https://infra.mail.ru:3535/v3/" 
        login: "example@test" 
        password: "123456example" 
        project_id: "777e19938fe947666d5d3be5c00053d7" 
        zone: "example.com" 
        own_ip: "213.213.213.213" 
        is_self_maintenance: true 
        driver: "vkcloudns" 
        protocol: "tcp" 
        port: 80
        timeout: 180 
    ```

    Мұнда:

    - `url`: Publicdns API эндпоинты.
    - `auth_url`: авторизацияға арналған URL, **API арқылы қолжеткізу** қойындысындағы **Auth URL** параметрі.
    - `login`: пайдаланушы логині, **API арқылы қолжеткізу** қойындысындағы **Username** параметрі.
    - `password`: пайдаланушы құпиясөзі.
    - `project_id`: жоба идентификаторы, **API арқылы қолжеткізу** қойындысындағы **Project ID** параметрі.
    - `zone`: бақылануы тиіс және ол үшін A-жазбалар жасалатын жария DNS-аймақ.
    - `own_ip`: сервис данасы орналасқан ВМ-ның IPv4-мекенжайы.
    - `is_self_maintenance`:
      - `true` — мақсатты IP-мекенжай мен DNSBalancer сервисінің IP-мекенжайы бірдей.
      - `false`— мақсатты IP-мекенжай мен DNSBalancer сервисінің IP-мекенжайы әртүрлі.
    - `driver`: DNS-серверге арналған бэкенд түрі. Тек `vkcloudriver` қолдау көрсетіледі.
    - `protocol`: IP-мекенжайды тексеру хаттамасы:
      - `icmp` — `ICMP Echo Request` арқылы тексеру, `ping` утилитасы сияқты жұмыс істейді.
      - `tcp` — мақсатты IP-мекенжаймен TCP-қосылым орнату арқылы тексеру.
    - `port`: тексерілуі қажет порт.
    - `timeout`: тексерулер арасындағы уақыт, секундпен, ұсынылатын мәні 180-тен 360-қа дейін.

1. Қадамдарды әр ВМ үшін қайталаңыз.

## {heading(3. Демонды жасап, іске қосыңыз)[id=dns-dnsbalancer-daemon]}

1. ВМ-лардың бірінде терминал сессиясын ашыңыз.
1. Келесі пәрменнің көмегімен `dnsbalancer.service` файлын жасаңыз:

   ```console
   sudo vim /etc/systemd/system/dnsbalancer.service
   ```  

1. Жасалған файлға келесі жолдарды қосыңыз:

    ```console
    [Unit]
    Description=Service for checking A-Records into Zone
    After=network.target

    [Service]
    Type=simple
    WorkingDirectory=/usr/local/bin
    ExecStart=/usr/local/bin/dnsbalancer
    Restart=always

    [Install]
    WantedBy=multi-user.target

1. Келесі пәрменнің көмегімен `systemd` процесін қайта жүктеңіз:

   ```console
   sudo systemctl daemon-reload
   ```

1. Келесі пәрменнің көмегімен демонды іске қосыңыз:

   ```console
   sudo systemctl start dnsbalancer.service
   ```

1. Демон жүйе жүктелген кезде автоматты түрде іске қосылуы үшін пәрменді орындаңыз:

   ```console
   sudo systemctl enable dnsbalancer.service
   ```

1. Қадамдарды әр ВМ үшін қайталаңыз.

Нәтижесінде сервис DNS-аймаққа тек қолжетімді ВМ-лардың A-жазбаларын береді, сондықтан қолданбаға барлық сұраулар тек белсенді серверге жіберіледі. Тіпті бір немесе екі ВМ өшірілген жағдайда да қолданба жұмысын жалғастырады.

## {heading(Пайдаланылмайтын ресурстарды жойыңыз)[id=dns-dnsbalancer-delete]}

Егер жасалған ВМ-лар енді қажет болмаса, оларды {linkto(../../../../computing/iaas/instructions/vm/vm-manage#iaas-vm-manage-delete)[text=жойыңыз]}.
