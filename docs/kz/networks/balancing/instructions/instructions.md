# {heading(Қадамдық нұсқаулықтар)[id=balancing-instructions]}

{include(/kz/_includes/_translated_by_ai.md)}

{ifdef(public)}
Сервисте негізгі операцияларды орындау бойынша қысқаша қадамдық нұсқаулықтар.
{/ifdef}