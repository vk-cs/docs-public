1. `kubectl` көмегімен {linkto(../../connect/kubectl#k8s-kubectl)[text=кластерге қосылыңыз]}.

1. Мәселе байқалатын подтың оқиғаларын келесі команданың көмегімен шығарыңыз:

   ```console
   kubectl describe pod <ИМЯ_ПОДА> -n <ПРОСТРАНСТВО_ИМЕН> | sed -n '/Events/,$p'
   ```
1. `Events` бөлімінде ұқсас жазбалардың бар-жоғын тексеріңіз:
