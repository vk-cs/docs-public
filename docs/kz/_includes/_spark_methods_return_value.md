{cut(K8sClusterInfo типіндегі объектілер өрістерінің сипаттамасы)}

[cols="1,1,5", options="header", width=100%]
|===
|Өріс
|Түрі
|Сипаттамасы

|`id`

(міндетті)
|`str`
|Cloud Spark кластерінің бірегей идентификаторы

|`name`

(міндетті)
|`str`
|Cloud Spark кластерінің бірегей атауы

|`k8s_id`

(міндетті емес)
|`str`
|Cloud Spark кластеріне байланыстырылған Kubernetes кластерінің идентификаторы. Оны жеке кабинеттегі **Контейнеры → Кластеры Kubernetes** бөлімінен табуға болады.

`None`, егер кластер байланыстырылмаған болса (мысалы, кластерді құру сұрауы жіберілгеннен кейінгі алғашқы секундтарда)

|`status`

(міндетті)
|`str`
|Cloud Spark кластерінің күйі. Қабылданатын мәндер:

- `CREATING` — кластерді құру;
- `CREATING_REGISTRY` — кластерді дайындау;
- `INSTALL_SPARK_OPERATOR` — кластерді дайындау;
- `PROVISIONING` — кластерді дайындау;
- `RECONCILING` — кластерді дайындау;
- `CREATE_FAILED` — кластерді құру қателікпен аяқталды;
- `ERROR` — қате;
- `STARTING` — кластерді іске қосу;
- `RUNNING` —  кластер іске қосылған;
- `FREEZING` — кластерді тоқтату;
- `SHUTOFF` — кластер тоқтатылған;
- `DELETING` — кластерді жою

|`status_reason`

(міндетті емес)
|`str`
|Cloud Spark кластерінің ағымдағы күйге ауысу себебі туралы ақпарат, мысалы, `status == CREATE_FAILED` болған жағдайда қате сипаттамасы.

`None`, егер кластердің бүкіл жұмыс істеу кезеңінде қателер болмаған болса

|`s3_bucket_name`

(міндетті емес)
|`str`
|Cloud Spark кластеріне байланыстырылған [бакет атауы](/kz/storage/s3/concepts/about#bucket). Оны жеке кабинеттегі **Объектное хранилище → Бакеты** бөлімінен табуға болады.

`None`, егер кластер үшін бакет әлі жасалмаған болса

|`docker_registry_id`

(міндетті)
|`str`
|Cloud Spark кластеріне байланыстырылған DockerRegistry инстансының идентификаторы. Оны жеке кабинеттегі **ML Platform → Инстансы** бөлімінен табуға болады.

|`history_server_url`

(міндетті емес)
|`str`
|Spark History Server серверінің URL мекенжайы.

`None`, егер сервер әлі жасалмаған болса

|`inactive_min`

(міндетті)
|`int`
|Соңғы белсенді тапсырма аяқталған сәттен бастап өткен минуттар саны

|`suspend_after_inactive_min`

(міндетті)
|`int`
|Кластер пайдаланылмаса, осы уақыт минутпен өткеннен кейін ол ұйқы режиміне өтеді.

`None`, егер кластердің әрекетсіздігінің шекті уақыты орнатылмаған болса

|`delete_after_inactive_min`

(міндетті)
|`int`
|Кластер пайдаланылмаса, осы уақыт минутпен өткеннен кейін ол жойылады.

`None`, егер кластердің әрекетсіздігінің шекті уақыты орнатылмаған болса

|`docker_registry`

(міндетті емес)
|`VmDetailInfo`
|Cloud Spark кластерінің DockerRegistry инстансына байланыстырылған ВМ туралы толық ақпарат.

`None`, егер DockerRegistry инстансы әлі жасалмаған болса
|===

{/cut}

{cut(VmDetailInfo типіндегі объектілер өрістерінің сипаттамасы)}

[cols="1,1,5", options="header", width=100%]
|===
|Өріс
|Түрі
|Сипаттамасы

|`availability_zone`

(міндетті)
|`str`
|ВМ қолжетімділік аймағы. Қабылданатын мәндер: `MS1`, `GZ1`

|`volumes`

(міндетті)
|`list[MLPlatformVolumeIn]`
|ВМ-ге қосылған дискілер тізімі
|===

{/cut}

{cut(MLPlatformVolumeIn типіндегі объектілер өрістерінің сипаттамасы)}

[cols="1,1,5", options="header", width=100%]
|===
|Өріс
|Түрі
|Сипаттамасы

|`size`

(міндетті)
|`int`
|Диск өлшемі

|`volume_type`

(міндетті)
|`str`
|Диск түрі

|`imageRef`

(міндетті емес)
|`str`
|Диск бейнесіне сілтеме

|`availability_zone`

(міндетті)
|`str`
|ВМ қолжетімділік аймағы. Қабылданатын мәндер: `MS1`, `GZ1`

|`cinder_id`

(міндетті емес)
|`str`
|Қосылған дискінің идентификаторы. Оны жеке кабинеттегі **Облачные вычисления → Диски** бөлімінен табуға болады.

`None`, егер диск әлі жасалмаған болса
|===

{/cut}
