# {heading(Қадамдық нұсқаулықтар)[id=event-log-instructions]}

{include(/kz/_includes/_translated_by_ai.md)}

{ifdef(public)}
Оқиғалар журналымен жұмыс істеу және SIEM жүйеңізді {var(cloud)} платформасына қосу жөніндегі нұсқаулықтар.
{/ifdef}
