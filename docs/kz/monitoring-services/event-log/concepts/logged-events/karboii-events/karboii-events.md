# {heading(Karboii компонентінің оқиғалары)[id=event-log-karboii]}

{include(/kz/_includes/_translated_by_ai.md)}

Сақтық көшірме жасау {linkto(../../../../../storage/backups/concepts/about#backup-about)[text=қызметі]} Cloud Audit қызметіне жіберетін оқиғалар.

Толық атауда `vkcloud.cloud_backup` префиксі бар.

## {heading(Домендік оқиғалар)[id=event-log-domain-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=domain-events]}

## {heading(API-ге сұраулар оқиғалары)[id=event-log-api-events]}

{include(../../../../../_includes/_backup_logged_events.md)[tags=api-events]}
