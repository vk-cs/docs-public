# {heading(Кластерді басқару)[id=mk8s-manage-cluster]}

{include(/kz/_includes/_translated_by_ai.md)}

{note:warn}
Managed Containers қызметі арқылы {linkto(../../../../data-platform#data-platform-main)[text={var(data-p)}]}. Ол үшін жеке кабинеттің {var(data-p)} {var(cloud)} бөлімін пайдаланыңыз.
{/note}

## {heading(Кластерді іске қосу немесе тоқтату)[id=mk8s-manage-cluster-start-stop]}

### {heading(Кластерді іске қосу)[id=mk8s-manage-cluster-start]}

{tabs}

{tab(Жеке кабинет)}

Бұл топтық операция: қажет болса, жалаушалар арқылы таңдап, бірден бірнеше тоқтатылған кластерді іске қосуға болады.

Кластерді іске қосу үшін:

1. [VK Cloud жеке кабинетіне өтіңіз](https://kz.cloud.vk.kz/app/).
1. Қажетті кластер орналасқан жобаны таңдаңыз.
1. **Кластеры Kubernetes → Кластеры Kubernetes** бөліміне өтіңіз.
1. Жалауша арқылы қажетті кластерді таңдаңыз.
1. **Запустить** түймесін басыңыз.
1. Операцияның орындалуын растаңыз.

{/tab}

{/tabs}

### {heading(Кластерді тоқтату)[id=mk8s-manage-cluster-stop]}

{tabs}

{tab(Жеке кабинет)}

Бұл топтық операция: қажет болса, жалаушалар арқылы таңдап, бірден бірнеше белсенді кластерді тоқтатуға болады.

Кластерді тоқтату үшін:

1. [VK Cloud жеке кабинетіне өтіңіз](https://kz.cloud.vk.kz/app/).
1. Қажетті кластер орналасқан жобаны таңдаңыз.
1. **Кластеры Kubernetes → Кластеры Kubernetes** бөліміне өтіңіз.
1. Жалауша арқылы қажетті кластерді таңдаңыз.
1. **Остановить** түймесін басыңыз.
1. Операцияның орындалуын растаңыз.

{/tab}

{/tabs}

## {heading(Кластер туралы ақпарат алу)[id=mk8s-manage-cluster-get-info]}

Іске қосылған және тоқтатылған кластерлер үшін әртүрлі ақпарат қолжетімді.

{tabs}

{tab(Жеке кабинет)}

1. [VK Cloud жеке кабинетіне өтіңіз](https://kz.cloud.vk.kz/app/).
1. Қажетті кластер орналасқан жобаны таңдаңыз.
1. **Кластеры Kubernetes → Кластеры Kubernetes** бөліміне өтіңіз.
1. Қажетті кластердің атауын басыңыз. Ақпарат бар бет ашылады.

{/tab}

<!-- удалена таба Terraform, при работе над задачей по TF забрать её из k8s и подставить нужные значения -->

{/tabs}

## {heading(Кластерге қосылу үшін реквизиттерді алу)[id=mk8s-manage-cluster-get-requisites]}

Қолжетімді реквизиттер:

- `kubectl` конфигурация файлы;
- Headlamp үшін құпиясөз (оны тек кластер іске қосылған кезде алуға болады).

Бұл реквизиттерді алу операциялары {linkto(../../connect#mk8s-connect)[text=Кластерге қосылу]} бөлімінде толық сипатталған.

## {heading(master-түйіндер үшін виртуалды машина түрін өзгерту)[id=mk8s-manage-cluster-change-master-type]}

Бұл операция {linkto(../scale#mk8s-instructions-scale)[text=Кластер түйіндерін масштабтау]} бөлімінде толық сипатталған. Оны тек кластер іске қосылған кезде орындауға болады.

## {heading(Кластерді жою)[id=mk8s-manage-cluster-delete]}

{tabs}

{tab(Жеке кабинет)}

Бұл топтық операция: қажет болса, жалаушалар арқылы таңдап, бірден бірнеше кластерді жоюға болады.

Кластерді жою үшін:

1. [VK Cloud жеке кабинетіне өтіңіз](https://kz.cloud.vk.kz/app/).
1. Қажетті кластер орналасқан жобаны таңдаңыз.
1. **Кластеры Kubernetes → Кластеры Kubernetes** бөліміне өтіңіз.
1. Қажетті кластер үшін мына әрекеттердің бірін орындаңыз:
   - Жалауша арқылы кластерді таңдаңыз, содан кейін **Удалить** түймесін басыңыз.
   - Кластер үшін ![](../../../../assets/more-icon.svg "inline") белгішесін басып, **Удалить кластер** тармағын таңдаңыз.
1. Пайда болған терезеде:
   1. Қажет болса, кластерді оның дискілерімен бірге жоюға арналған тиісті опцияны таңдаңыз.
   1. **Удалить кластер** түймесін басыңыз.

{/tab}

{/tabs}
