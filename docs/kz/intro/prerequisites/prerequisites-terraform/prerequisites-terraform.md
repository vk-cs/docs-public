# {heading(Terraform орнату және инициализациялау)[id=terraform-install]}

{include(/kz/_includes/_translated_by_ai.md)}

## {heading(Terraform орнату)[id=terraform-install-prepare]}

{include(../../../_includes/_terraform-install.md)[tags=terraform-install-prepare,prerequisites-terraform-install-prepare]}

## {heading(Terraform инициализациялау)[id=terraform-install-init]}

{include(../../../_includes/_terraform-install.md)[tags=terraform-install-init]}

## Әрі қарай не істеу керек?

1. Ресурстарды жасау үшін {linkto(../../../tools-for-using-services/terraform/reference/configuration#terraform-resource-config)[text=конфигурация файлдарын]} алу мақсатында әкімшіге хабарласыңыз.
1. Terraform көмегімен {linkto(../../../tools-for-using-services/terraform/instructions/create-resources#terraform-create-resources)[text=ресурсты жасаңыз]}.
