# {heading(OpenStack CLI-ге қосылу)[id=prerequisites-openstack-cli]}

{include(/kz/_includes/_translated_by_ai.md)}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-intro]}

## {heading({counter(prerequisites-cli)}. OpenStack клиентін орнатыңыз)[id=prerequisites-openstack-install]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install]}

## {heading({counter(prerequisites-cli)}. Қосымша пакеттерді орнатыңыз)[id=prerequisites-openstack-install-package]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-install-package]}

## {heading({counter(prerequisites-cli)}. Аутентификациядан өтіңіз)[id=prerequisites-openstack-authorize]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-authorize]}

## {heading({counter(prerequisites-cli)}. OpenStack CLI-дің жұмысқа дайындығын тексеріңіз)[id=prerequisites-openstack-check]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-check]}

## {heading(OpenStack CLI командаларының мысалдары)[id=prerequisites-openstack-example]}

{include(../../../_includes/_openstack-cli.md)[tags=openstack-example]}
